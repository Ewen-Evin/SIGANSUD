<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sigansud"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Login Activity (launcher) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (après connexion) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- Animaux par espèce -->
        <activity
            android:name=".activities.AnimauxActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Détail d'un animal -->
        <activity
            android:name=".activities.AnimalDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.AnimauxActivity" />

        <!-- Saisie des repas -->
        <activity
            android:name=".activities.SaisieRepasActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Menus du jour -->
        <activity
            android:name=".activities.MenusActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Historique des repas -->
        <activity
            android:name=".activities.HistoriqueActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

    </application>

</manifest>